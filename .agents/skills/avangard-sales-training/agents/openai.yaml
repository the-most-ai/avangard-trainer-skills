interface:
  display_name: "Продажи и работа с клиентом — тренажёр"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "most-trainer"
      description: "AvangardAI voice-training MCP (create_training_profile)"
      transport: "streamable_http"
      url: "https://api-trainer.avangardai.ru/mcp"
